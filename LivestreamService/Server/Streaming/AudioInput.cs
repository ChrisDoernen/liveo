namespace Server.Streaming
{
    public class AudioInput
    {
        public readonly string InputIdentifier;

        public AudioInput(string inputIdentifier)
        {
            this.InputIdentifier = inputIdentifier;
        }
    }
}